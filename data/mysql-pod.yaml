apiVersion: v1
kind: Pod
metadata:
  name: acme-mysql
  labels:
    name: acme-mysql
spec:
  containers:
  - name: mysql
    image: mysql:5.7
    env:
    - name: MYSQL_ROOT_PASSWORD
      valueFrom:
        secretKeyRef:
          name: mysql-pass
          key: password
    ports:
    - name: mysql
      containerPort: 3306
      protocol: TCP
    volumeMounts:
    - name: acme-mysql-storage
      mountPath: /var/lib/mysql
  volumes:
  - name: acme-mysql-storage
    emptyDir: {}